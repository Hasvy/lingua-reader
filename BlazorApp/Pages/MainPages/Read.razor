@using Objects;
@using BlazorApp.Components.DisplayBooks;
@using Objects.Constants;

@attribute [Route(Routes.Read)]

@if (_isLoading)
{
     <RadzenProgressBar ProgressBarStyle="ProgressBarStyle.Primary" Value="100" ShowValue="false" Mode="ProgressBarMode.Indeterminate" />
    @* <RadzenIcon class="centered-element" Icon="sync" Style="animation: reverse rotation 750ms linear infinite; font-size: 50px"/> *@
}
else
{
    @if (BookFormat == ConstBookFormats.epub)
    {
        <DisplayEpubBook BookId="@BookId" BookLanguage="@BookLang" />
    }

    @if (BookFormat == ConstBookFormats.pdf)
    {
        <DisplayPdfBook BookId="@BookId" BookLanguage="@BookLang" BookFormat="@BookFormat" />
    }

    @if (BookFormat == ConstBookFormats.txt)
    {
        <DisplayPdfBook BookId="@BookId" BookLanguage="@BookLang" BookFormat="@BookFormat" />
    }

    @if (BookFormat == ConstBookFormats.fb2)
    {

    }

    @if (BookFormat == ConstBookFormats.mobi)
    {

    }
}