@using Objects;
@using Objects.Constants;

@attribute [Route(Routes.Settings)]

<RadzenHeading Text="Settings page"></RadzenHeading>

<RadzenDropDown class="horizontal-dropdown" @bind-Value="UserMainLang" TValue="string" Data="ConstLanguages.TargetLanguagesSet" Change="@(() => ChangeUserLang(UserMainLang))">
    <Template Context="language">
        @language
        @* <img class="dropdown-item" src="@GetFlagPath(language)" /> *@
    </Template>
</RadzenDropDown>